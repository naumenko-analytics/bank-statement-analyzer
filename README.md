# bank-statement-analyzer
Python tool that reads raw bank statements, automatically categorizes transactions, and generates a profit and loss summary in Excel, rule-based classification with an AI fallback for messy descriptions.
